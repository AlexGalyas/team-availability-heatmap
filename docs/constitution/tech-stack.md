# Tech Stack — Frontend
- pnpm
- Next.js (App Router) + React + TypeScript — single project at repo root
- Styling: SCSS Modules (mixins, variables, reusable partials)
- Global state: Redux Toolkit; client data/caching/mutations: RTK Query
- Server / initial data: Server Components + route handlers
- Forms: Formik + Yup
- Tests: Vitest + React Testing Library, Playwright (e2e)
- Deploy: <ще не вирішено>
